{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "MixLab Cutter desktop capability for local engine startup, folder selection, and folder reveal.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
