# M12.4 管理端真实长跑验收报告

验收日期：2026-05-03
素材来源：`/Volumes/Allen移动硬盘/source-library`
公共素材库：`/tmp/mixlab-live-worker-library.KdzZK0`
管理端：`http://127.0.0.1:5174/#/preprocess-jobs`
管理 API：`http://127.0.0.1:3889`
剪辑 API：`http://127.0.0.1:3789`

## 验收目标

- 验证管理端真实预处理流水线可以持续处理当前素材来源中的排队视频。
- 验证预处理页无需手动刷新即可看到状态变化。
- 验证单视频完成后自动发布到当前索引并对剪辑端可见。
- 验证安全边界停止后可以继续，不破坏已完成产物。
- 验证管理端 API 不暴露 DashScope 临时签名 URL、对象 key 或语音识别敏感结果地址。

## 基线采样

M12.4 启动前，管理 API 显示当前素材库共有 41 个原视频，其中 18 个已可用、23 个在预处理队列中、0 个处理中、0 个未处理、0 个失败。管理端页面显示当前索引为 `v000016`，队列预计完成时间约为 2026-05-03 14:02，运行负荷提示 CPU 和内存偏高。

启动前同步修复了一个安全边界问题：`/api/admin/preprocess/jobs` 和 `/api/admin/preprocess/supervisor/status` 不再返回临时 ASR 对象地址、签名 URL 或逐条 ASR 结果，只返回 `total_claimed_count`、`succeeded_count`、`failed_count` 三个汇总值。

## 执行记录

- 2026-05-03T13:52:28.969Z：在管理端页面点击“启动预处理流水线”，服务进入“运行中”。
- 2026-05-03T13:54:56Z：正在处理 `V000025 / C2085`，阶段为“语音识别”，进度约 53%；完成数从 18 增至 23，队列剩 17。
- 2026-05-03T13:56:06Z：正在处理 `V000029 / C2089`，完成数 27，队列剩 13，失败 0。
- 2026-05-03T13:57:41.917Z：第一段流水线在安全边界停止，汇总为领取 15、成功 15、失败 0；此时完成 33，队列剩 8。
- 2026-05-03T14:01:22.338Z：再次点击“启动预处理流水线”，从剩余队列继续处理，没有重跑已完成视频。
- 2026-05-03T14:02:20Z：正在处理 `V000038 / C2098`，完成数 36，队列剩 4，失败 0。
- 2026-05-03T14:04:37.971Z：最后一个视频 `V000042 / C2102` 完成。
- 2026-05-03T14:04:43Z：服务回到“未运行”，最终完成 41，队列 0，处理中 0，失败 0。

浏览器页面在整个过程中自动刷新，不需要手动刷新即可看到：当前视频、阶段、进度、最近完成、队列数、索引版本和负荷建议变化。

## 产物抽查

抽查 `V000035`、`V000038`、`V000042` 三个 M12.4 新完成视频，均满足：

| 视频 | 标题 | 状态 | 文案字数 | 段落数 | 关键帧 | 完成时间 |
|---|---:|---|---:|---:|---:|---|
| `V000035` | C2095 | ready / 剪辑师可见 | 3781 | 152 | 60 | 2026-05-03T14:01:40.294Z |
| `V000038` | C2098 | ready / 剪辑师可见 | 4999 | 180 | 60 | 2026-05-03T14:02:54.685Z |
| `V000042` | C2102 | ready / 剪辑师可见 | 4113 | 163 | 60 | 2026-05-03T14:04:37.971Z |

三条视频目录均存在 `source-video.json`、`preprocess-job.json`、`transcript.json`、`subtitles.srt`、`cover.jpg`、`keyframes.json`。

索引抽查结果：当前索引为 `v000039`，`index-manifest.json` 中 `ready_video_count = 41`，且包含 `V000035`、`V000038`、`V000042`；`index.sqlite` 文件大小约 10.96 MB。

剪辑端可见性抽查：使用已审核剪辑师 `CU000001` 登录剪辑 API，`/cutter/source-library` 返回 41 个可用原素材；`/cutter/source-videos/V000042` 可读取完整文案、163 个文案段落和 60 个关键帧；`/cutter/source-search?query=课程` 返回 10 个分组搜索结果。

## 发现的问题与处理

1. 管理 API 曾暴露 supervisor 内部 `last_result.items`，其中可能包含临时 ASR 对象地址和签名结果地址。已在 `packages/admin-api/src/index.ts` 增加 API 边界脱敏，只保留汇总数量；已新增自动化测试覆盖。
2. 页面和 API 的任务记录展示 `.mixlab-library/logs/Vxxxxxx.log` 路径，但当前真实产物目录下未发现独立日志文件。M12.4 不阻塞验收，因为 `preprocess-job.json` 已记录状态和时间；建议 M13 把“日志入口”改成真实可打开的处理记录或去掉虚假日志路径。
3. 运行时 CPU/内存持续偏高，管理端能显示“需处理”负荷建议。当前并发为 1，真实长跑未失败；M13 需要增加更清晰的负荷阈值解释和处理建议。

## 最终结论

M12.4 通过。管理端已能从页面真实启动预处理流水线，处理 23 个排队视频，最终 41 个原视频全部 ready，失败 0，当前索引自动发布到 `v000039` 并对剪辑端可见。安全脱敏已补齐并有测试保护。
