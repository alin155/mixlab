# MixLab V3 Spec Traceability

This file is the delivery baseline. Implementation work must reference these IDs before changing code.

Source specification directory:

```text
/Users/allen/Desktop/MixLab_V3_开发交付规格书
```

Hi-fi references:

```text
/Users/allen/Desktop/MixLab_V3_开发交付规格书/assets/ui/cutter-workbench-apple-hig.png
/Users/allen/Desktop/MixLab_V3_开发交付规格书/assets/ui/admin-console-apple-hig.png
```

## Status Legend

| Status | Meaning |
|---|---|
| validated-core | Core behavior exists and has automated tests. |
| preview-ui | A development preview exists but is not final product UI. |
| partial | Some supporting behavior exists, but the requirement is not accepted. |
| not-started | No formal product implementation exists yet. |
| accepted | Implemented and accepted against the spec. |

## Product And Scope

| ID | Source | Requirement | Current Status | Acceptance |
|---|---|---|---|---|
| PROD-001 | `00_交付总说明.md` | Follow the delivery spec as binding scope; changes require written confirmation. | accepted | Every implemented requirement now maps to this traceability file; the only stack deviation is covered by accepted written change request `docs/change-request-arch-003-typescript-admin-api.md`, and V1 scope is guarded by `docs/v1-scope-guard.md` plus automated scope-contract tests. |
| PROD-002 | `01_产品定位与边界.md` | First version supports search, full transcript reading, continuous selection, local cutting, and reusable exports. | accepted | `npm run smoke:cutter-api-web` verifies searchd-backed keyword hits, full transcript detail, UI text selection, local clip export, local-library refresh, and reusable `E000001` material; `npm run smoke:searchd-concurrency` proves 50 editors can search/read/select/cut concurrently with p95 search 77.5ms, detail 65.2ms, and cut submit 370ms; `npm run smoke:searchd-scale` proves 2,000 videos / 48,000 segments with p95 API search 558.5ms and detail 7ms; `docs/acceptance/local-web-real-nas.md` records the local Web preflight against `/Volumes/MixLab/PublicLibrary`, where Admin Web and Cutter Web proved the real NAS search-to-full-transcript-to-local-cut loop plus automated desktop and compact route-level admin/cutter layout overflow checks, and `npm run smoke:real-nas-50-editor` proved the current source-machine real NAS 50-editor rehearsal on `v003022` with 50 active users, 50 distinct source videos, 5 distinct search queries (`现金流`, `利润`, `客户`, `增长`, `品牌`), 3,022 indexed source videos, 403,605 indexed transcript segments, search p95 609.1ms, detail p95 478.7ms, cut p95 160.7ms, and zero search failures under the 1000ms local Web phase SLA. |
| PROD-003 | `02_角色与双端关系.md` | Admin writes/governs public library; cutter reads public library and writes local workspace only. | accepted | API tests and `smoke:cutter-api-web` prove cutter reads public material through the Cutter API/searchd while local cuts write workspace `export-clips`, local-asset, and project files; the smoke also asserts public-library local-clip, `E000001`, export, and project paths are not created by the cutter app. |
| PROD-004 | `15_不做清单.md` | Do not add out-of-scope AI recommendations, collaboration, complex permissions, or cloud material management in version one. | accepted | `docs/v1-scope-guard.md` defines allowed V1 capabilities, explicit non-scope capabilities, and the written-change-request rule; `validateFirstVersionScopeClaims` tests reject AI recommendation, collaboration, complex-permission, and cloud-material claims unless they are documented as non-scope or change requests. |

## Architecture And Runtime

| ID | Source | Requirement | Current Status | Acceptance |
|---|---|---|---|---|
| ARCH-001 | `03_硬件与部署环境.md` | Develop first on one Mac plus mobile disk; Mac mini and NAS are later stages. | accepted | Stage 0R established the spec-driven baseline; M12.4 completed the single-Mac plus mobile-disk long run against `/Volumes/Allen移动硬盘/source-library`; Windows and NAS remain separate later-stage acceptance gates under ACC-008 and ACC-009. |
| ARCH-002 | `04_技术架构方案.md` | Keep API boundary between UI and backend/index implementation. | accepted | Cutter web talks to typed API clients and the Cutter API/searchd boundary; `npm run smoke:cutter-api-web` verifies search/detail/media/export through HTTP API paths, and full tests keep backend/index access out of UI state. |
| ARCH-003 | `05_技术栈选型.md` | Formal admin backend is Go; formal cutter desktop is Tauri + React + TypeScript. | accepted | Accepted change request `docs/change-request-arch-003-typescript-admin-api.md` keeps the V1 Admin API in TypeScript/Node with API-boundary guardrails, while the cutter desktop remains Tauri + React + TypeScript with sidecar/runtime tests. |
| ARCH-004 | `12_权限与路径解析规格.md` | Portable manifests must use library id, source video id, and relative paths rather than public-library absolute paths. | accepted | Protocol and desktop-runtime tests cover macOS paths, Windows drive paths, UNC paths, Chinese/space paths, traversal rejection, and workspace-relative export/local asset paths; smoke confirms cutter outputs stay workspace-relative. |
| ARCH-005 | `22_运行时依赖与ASR配置.md` | FFmpeg/FFprobe are bundled or configurable; users are not asked to manually install FFmpeg for first version. | accepted | `ffmpeg-core` resolves explicit env paths or bundled static FFmpeg/FFprobe, Doctor and Cutter settings expose status/source, Windows runtime asset tests copy bundled binaries for Tauri, and `smoke:cutter-api-web` performs a real FFmpeg-backed export without manual install prompts. |
| ARCH-006 | `22_运行时依赖与ASR配置.md` | DashScope API key and ASR logs are redacted. | accepted | ASR/OSS/admin tests prove Authorization headers, signed URLs, runtime settings, Doctor reports, approval tokens, and failure summaries are redacted; M12.4 also proves admin supervisor APIs expose only aggregate ASR run counts. |

## Public Library And Preprocessing

| ID | Source | Requirement | Current Status | Acceptance |
|---|---|---|---|---|
| LIB-001 | `06_公共素材库协议.md` | Public library directory structure is fixed and NAS-compatible. | accepted | Scanner, Admin init, path-check, and smoke tests create/validate `source-videos`, `.mixlab-library`, versioned index, video artifact, and `library.json` structures using portable paths. |
| LIB-002 | `06_公共素材库协议.md` | Each source video has stable `source_video_id`. | accepted | Scanner tests preserve existing `V000001` style ids across rescans, append new ids deterministically, and prune only manifests outside the current enabled source folders. |
| LIB-003 | `06_公共素材库协议.md`, `19_增量预处理与可见性规则.md` | Only `ready` videos are cutter visible/searchable. | accepted | Protocol, library-fs, cutter API, search index, and smoke tests prove unprocessed, queued, processing, failed, incomplete, and index-required videos stay hidden while ready published videos are visible/searchable. |
| LIB-004 | `19_增量预处理与可见性规则.md` | Partially preprocessed library is usable; ready videos appear while later videos continue processing. | accepted | M12.4 live run processed 23 queued videos incrementally: ready count advanced while later videos stayed queued/processing, and cutter catalog reached 41 visible videos after automatic publication. |
| LIB-005 | `07_数据模型与Manifest.md` | `library.json`, `source-video.json`, `transcript.json`, `keyframes.json`, `clip-list.json`, and `export-clip.json` match the schema. | accepted | Shared protocol validators now cover source-video visibility/artifact paths, library counts, local clips, export clips, and `clip-list.json`; `cutter-local` enforces `validateClipListManifest` on write/read, including item ids/order, safe source paths, time ranges, cut mode, pre/post roll, and local export reuse sources. |
| LIB-006 | `20_开发落地路线与技术护栏.md` | Ready publication is atomic and follows transcript/SRT/keyframes/cover/index validation before visibility. | accepted | Ready publisher and search integration tests cover complete artifact publication, incomplete artifact blocking, immutable SQLite index packages, `current.json` switching, and cutter catalog/search refresh after publication. |
| LIB-007 | `20_开发落地路线与技术护栏.md` | Versioned read-only index packages and `current.json` are used. | accepted | M6 publishes real SQLite transcript index packages and cutter search reads the current pointer. |
| LIB-008 | `22_运行时依赖与ASR配置.md` | ASR uses DashScope temporary upload storage rather than local file paths. | accepted | `asr-core` and preprocess tests validate DashScope temporary upload policy flow, `oss://` submission with OSS resource resolution, local-path rejection, transcript conversion, redaction, and preprocess-compatible temporary audio uploaders. |

## Management End

| ID | Source | Requirement | Current Status | Acceptance |
|---|---|---|---|---|
| ADMIN-001 | `08_素材库管理端规格.md`, `21_视觉与交互设计规范.md` | Admin dashboard shows restrained public-library governance status. | accepted | Admin dashboard loads public-library status, runtime settings, preprocessing supervisor summary, metrics, Doctor summary, current index pointer, and route-specific refreshes through the Admin API client; the restrained page shows production health, core search-to-cut readiness, 50-editor usage, warnings, and disk/runtime status without reverting to a marketing dashboard. |
| ADMIN-002 | `08`, `21` | Public library settings initialize and validate root, `source-videos`, `.mixlab-library`, id, protocol, permissions. | accepted | Admin API `/api/admin/library/init` creates the public root, `source-videos`, `.mixlab-library/videos`, source-transcript index directory, and `library.json`; path checks validate root/source/protocol/manifest read-write state, settings show library id/protocol/source folders/runtime policy, and the Admin settings page exposes a conditional first-run initialization repair entry when `.mixlab-library` or `library.json` is missing. |
| ADMIN-003 | `08`, `21` | Source video management edits public metadata: cover, tags, description, lecturer, course, category. | accepted | Admin source management edits title/tags/description/lecturer/course/category and replaces cover JPG/PNG/WebP through the Admin API/UI; updates persist to `source-video.json`, Admin serves the real image type, and Cutter library list/detail/cover routes expose the updated public metadata. |
| ADMIN-004 | `08`, `19`, `21` | Preprocessing task page shows active, queued, completed, failed/retry, stage, logs, error reason. | accepted | M12.4 proves real start/resume, active stage/progress, queued/completed/failed counts, and automatic refresh; M13 writes real `.mixlab-library/logs/V*.log` task records for claim/stage/complete/fail/publish transitions, exposes `/api/admin/preprocess/jobs/J*/log`, and renders selectable logs in the Admin preprocessing page. |
| ADMIN-005 | `08`, `20`, `21` | Index publication page shows current pointer, historical versions, validation, and atomic switch. | accepted | M13 folds index publication into the `预处理` page per M12.1: Admin API publishes待索引视频 through the real SQLite/current.json ready-publish path, validates `current.json`, `index-manifest.json`, and `index.sqlite`, exposes Chinese validation messages, and the Admin page shows current pointer, historical versions, validation details, and publish controls. |
| ADMIN-006 | `08`, `21`, `22` | Doctor checks public paths, manifests, artifacts, index, FFmpeg, ASR, permissions, counts, logs and exports JSON. | accepted | M13 extends Doctor core to validate public paths, `.mixlab-library` and log write permissions, library counts, source-video manifests/artifacts, current `current.json`/`index-manifest.json`/`index.sqlite` package metadata, FFmpeg/FFprobe, redacted ASR config, local clips, and lifecycle task logs; Admin API `/api/admin/doctor/export` writes the report JSON under `.mixlab-library/exports/doctor/`, and the Admin Doctor button calls the backend export action. |
| ADMIN-007 | `22` | Admin ASR settings manage provider, model, audio mode, key configured status, and last failure reason. | accepted | Admin settings persist DashScope API keys separately in `admin-runtime-secrets.json` without echoing them, runtime settings expose provider/model, saved audio mode, configured key state, temporary-upload mode, and latest ASR failure from real preprocess jobs, and `/api/admin/settings/test-asr` reports readiness without leaking secrets. |

## Cutter End

| ID | Source | Requirement | Current Status | Acceptance |
|---|---|---|---|---|
| CUTTER-001 | `09`, `21` | Cutter has independent pages: public source library, search/document, cut list, local library, cut queue, settings. | accepted | M4 cutter web app renders all cutter pages with screenshot acceptance. Tauri shell remains a later packaging step. |
| CUTTER-002 | `21` | Public source library is gallery-first like Apple TV/Photos/Finder Gallery, not a table or backend dashboard. | accepted | M4 public library page is gallery-first, ready-only, and shows admin-configured cover/tags/description. |
| CUTTER-003 | `09`, `10`, `21` | Source detail shows video, complete transcript, timestamps, highlights, continuous selection, right inspector. | accepted | M14 wires detail hash context, highlighted hit segments, continuous transcript selection, and one-span add-to-cut-list with automated tests and browser smoke. |
| CUTTER-004 | `10`, `21` | Search results are grouped by source video and open full transcript context; no sentence waterfall. | accepted | M14 search form calls the Cutter API through hash state, renders grouped source-video results, and opens full transcript context with hit segment ids. |
| CUTTER-005 | `09`, `11`, `21` | Cut list persists segment spans, ordering, cut mode, clear/delete, and submit. | accepted | M8 binds the cut-list UI to the M7 `clip-list.json` and `/cutter/cut-jobs` API flow while preserving ordering and cut modes. |
| CUTTER-006 | `09`, `11`, `21` | Cut queue shows pending/running/done/failed/retry and does not block search. | accepted | M8 binds the queue UI to `/cutter/cut-jobs`, adds refresh/run-next controls, and keeps source/search routes independent. |
| CUTTER-007 | `09`, `11`, `21` | Local library is independent, searchable, reusable, and shows source traceability. | accepted | UI state tests cover searchable local materials, API queue tests prove `E000001` exports can be reused as the source for `E000002`, Cutter API protects the project output directory open action, and the local library detail panel now opens the selected local clip's project output directory with visible feedback. |
| CUTTER-008 | `11`, `07` | Every export writes `export-clip.json`. | accepted | M7 tests prove completed workspace cuts write output video plus `export-clip.json` with source traceability. |
| CUTTER-009 | `22`, `21` | Settings show public mount path, local workspace, FFmpeg status, default cut mode, concurrency, Doctor entry. | accepted | Cutter settings render public mount path, local workspace, default source/orientation filters, default cut mode, concurrency, audio mode, display mode, and Doctor status; real runtime status tests cover approved Cutter API connection, workspace readiness, local clip count, FFmpeg status/source, and desktop bridge tests cover native engine startup and directory opening. |

## Acceptance And Delivery

| ID | Source | Requirement | Current Status | Acceptance |
|---|---|---|---|---|
| ACC-001 | `14_验收标准与测试剧本.md` | Management initialization test. | accepted | `packages/admin-api/src/index.test.ts` proves `/api/admin/library/init` creates the public root, `source-videos`, `.mixlab-library/videos`, index directory, and `library.json`; path checks pass with read/write permission validation, scan discovers unprocessed video, and dashboard metrics load. |
| ACC-002 | `14` | Preprocessing test with at least three videos. | accepted | M12.4 live run processed 23 videos; sampled videos have manifest, job record, transcript, SRT, cover, and keyframes. |
| ACC-003 | `14` | Cutter connection test. | accepted | `npm run smoke:cutter-api-web` starts searchd, Cutter API, and cutter-web; it proves library search/detail/source media are readable, local workspace export/local-asset/project files are written, and public library local-clip/export/project paths are not created. The local Web real NAS preflight additionally proves the running Cutter Web at `http://127.0.0.1:5177/` can connect through the Cutter API/searchd to `/Volumes/MixLab/PublicLibrary` and complete a local workspace cut without writing cutter outputs into the public NAS library. |
| ACC-004 | `14` | Incremental preprocessing visibility test with 10 videos. | accepted | M12.4 live run processed 23 queued videos with automatic incremental publication and cutter-visible ready catalog updates. |
| ACC-005 | `14` | Search and document test. | accepted | M14 focused tests and browser smoke prove query search, grouped results, detail context, highlighted transcript, and add-to-cut-list path. `npm run audit:local-web-sanity` with `MIXLAB_ADMIN_WEB_URL=http://127.0.0.1:5176/`, `MIXLAB_CUTTER_WEB_URL=http://127.0.0.1:5177/`, and `MIXLAB_SEARCHD_BASE_URL=http://127.0.0.1:3799/` proves the local Web cutter can search the real NAS index, Admin current index, searchd health, and Material Locator actual search results agree on the same index version, grouped public source candidates render, current hit segment id/time is exposed, and transcript header counters match structured hit-to-transcript evidence. |
| ACC-006 | `14` | Selection and cutting test. | accepted | `npm run smoke:cutter-api-web` proves UI transcript hit selection exports a local clip through the real Cutter API with precise selected text and millisecond range. The saved `docs/acceptance/artifacts/local-web-sanity.json` report proves the real NAS local Web loop selected text containing `现金流`, cut it locally, received `剪切完成 · 本地素材已更新 1`, and found the selected text in the local library while preserving local-before-public result ordering. |
| ACC-007 | `14` | Export reuse test. | accepted | `packages/cutter-api/src/index.test.ts` proves a completed `E000001` export can be queued as source material, cut into `E000002`, listed as a local clip, streamed, and cleaned up with the project. |
| ACC-008 | `14` | Windows test. | not-started | Requires a fresh Windows 10/11 target machine to verify drive letter, UNC, Chinese path, bundled FFmpeg, installer file name/version/SHA-256, desktop first-run, export parity, diagnostics, and artifact provenance; the Windows installer workflow now uploads both `mixlab-cutter-windows-exe` and `mixlab-target-evidence-kit`, testers can update target-machine facts with the PowerShell collector, use `npm run validate:windows-evidence -- docs/acceptance/evidence/windows-acc-008.json` as the ACC-008 preflight, and must finish with `npm run validate:target-evidence -- docs/acceptance/evidence/windows-acc-008.json docs/acceptance/evidence/nas-acc-009.json` once paired NAS evidence exists. |
| ACC-009 | `14` | NAS final test. | not-started | Requires a real SMB/NAS target to verify read-only public library access, 50-editor search/cut behavior, large public-library scale, NAS Docker management deployment, `deployment.preprocess_count_refresh_interval` from the deployed worker configuration plus matching worker-log `count_refresh_interval`, and artifact provenance with `deployment.image_tag` matching the recorded commit SHA; the Admin Docker workflow now uploads `mixlab-target-evidence-kit` beside the pushed GHCR images, testers can update NAS deployment facts with the shell collector, use `npm run validate:nas-evidence -- docs/acceptance/evidence/nas-acc-009.json` as the ACC-009 preflight, and must finish with `npm run validate:target-evidence -- docs/acceptance/evidence/windows-acc-008.json docs/acceptance/evidence/nas-acc-009.json` including a per-editor `multi_user_search_cut_report` proving 50+ unique users each completed the searchd/full-transcript-location/selection/local-clip loop against at least 2,000 indexed videos and 48,000 transcript segments. |
| DELIV-001 | `16_交付物清单.md` | Deliver admin app, cutter app, public library protocol, docs, tests, acceptance reports. | partial | Admin app, cutter app, shared public-library protocol, acceptance docs, automated tests, functional smoke, 50-editor concurrency smoke, scale smoke, searchd tests, local Web real NAS preflight record and saved report, local source-machine real NAS 50-editor report, Windows/NAS evidence templates, standalone collector kit, CI-uploaded `mixlab-target-evidence-kit` artifact, Windows installer workflow evidence-kit upload, Admin Docker workflow evidence-kit upload, validators, `npm run audit:delivery-readiness`, final combined target gate `npm run validate:target-evidence` with official `alin155/mixlab` Actions provenance, same-commit artifact provenance, attachment existence, OS-specific Windows screenshots, screenshot signature, JSON parse, NAS image-tag-to-commit match, NAS 2,000-video / 48,000-segment scale proof, NAS 50-editor p95/usage report, per-editor NAS search-hit-to-full-transcript-location proof, and text-redaction checks, and CI validation for committed final target evidence are present; final delivery is waiting only on real Windows and NAS acceptance evidence under ACC-008 and ACC-009. |
